import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hridoyroy.financetracker',
  appName: 'Finance Tracker',
  webDir: 'www'
};

export default config;
